<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vng.zalo.tdtai.zalo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".screens.lobby.fragments.chat_fragment.chat_activity.ChatActivity"
            android:parentActivityName=".screens.lobby.LobbyActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".screens.lobby.LobbyActivity"
            android:label="@string/title_activity_lobby_ativity2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".screens.login.LoginActivity"
            android:parentActivityName=".screens.intro.IntroActivity" />
        <activity android:name=".screens.intro.IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>